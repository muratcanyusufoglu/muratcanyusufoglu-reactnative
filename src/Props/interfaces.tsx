export interface ProductProps {
  avatar: string;
  category: string;
  createdAt: string;
  description: string;
  developerEmail: string;
  name: string;
  price: number;
  updatedAt: string;
}

export interface CategoryProps {
  createdAt: string;
  name: string;
  updatedAt: string;
}
